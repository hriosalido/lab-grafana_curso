cells:
  temp:
    dataRef: temperatura
    label:
      units: celsius
  hum:
    dataRef: humedad
  pres:
    dataRef: presion
mappings:
  - key: temperatura
    valueTemplate: la temperatura es ${value}ºC
